import { SeatingService } from './seating.service'; describe('SeatingService', () => { const service = new SeatingService(); it('assigns one seat per coupon in order', () => expect(service.assign([{ id: 'a', coupons: 2 }, { id: 'b', coupons: 1 }], 'ORDER')).toEqual([{ guestId: 'a', assignedSeats: ['M-01', 'M-02'] }, { guestId: 'b', assignedSeats: ['M-03'] }])); it('groups families', () => expect(service.assign([{ id: 'b', family: 'Zulu', coupons: 1 }, { id: 'a', family: 'Alpha', coupons: 1 }], 'FAMILY')[0].guestId).toBe('a')); });
